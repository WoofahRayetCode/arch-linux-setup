#Get disk names
sudo blkid | grep UUID=

#Make dirs to mount drives to
mkdir Games Storage

#Open fstab file
sudo nano /etc/fstab

#Game SSD
UUID=c16e2029-1787-4ee1-9144-62514d4a44b9 /home/ericparsley/Games btrfs defaults 0 0

#Storage HDD
UUID=76c86817-fb28-48c9-83b1-db5a0cd2b9fe /home/ericparsley/Storage btrfs defaults 0 0

#Reload to use next command
sudo systemctl daemon-reload

#Refresh stuff so I don't have to restart anymore :D
sudo mount -a

#Gain ownership of HDDs/SSDs
sudo chown -R -v ericparsley:ericparsley /home/ericparsley/#
