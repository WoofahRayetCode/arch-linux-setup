#Get disk names
sudo blkid | grep UUID=

#Make dirs to mount drives to
mkdir Storage Games

#Open fstab file
sudo nano /etc/fstab

#Games SSD
UUID=b82214a3-9569-45c4-81d0-d5284947ec0e /home/ericparsley/Games btrfs defaults 0 0

#Storage HDD
UUID=8f6e1db2-0c5e-4041-b352-c89dd8298dfc /home/ericparsley/Storage btrfs defaults 0 0

#Reload to use next command
sudo systemctl daemon-reload

#Refresh stuff so I don't have to restart anymore :D
sudo mount -a

#Gain ownership of HDDs/SSDs
sudo chown -R -v ericparsley:ericparsley /home/ericparsley/Games
sudo chown -R -v ericparsley:ericparsley /home/ericparsley/Storage
