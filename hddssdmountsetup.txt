#Get disk names
sudo blkid | grep UUID=

#Make dirs to mount drives to
mkdir Games Storage

#Open fstab file
sudo nano /etc/fstab

#Game SSD
UUID=d4cbdd82-70fc-4fec-a62c-d3f22976d8ce /home/ericparsley/Games ext4 defaults 0 0

#Storage HDD
UUID=13b33a8a-34fe-4556-83e7-f32cbf34d9da /home/ericparsley/Storage ext4 defaults 0 0

#Reload to use next command
sudo systemctl daemon-reload

#Refresh stuff so I don't have to restart anymore :D
sudo mount -a

#Gain ownership of HDDs/SSDs
sudo chown -R -v ericparsley:ericparsley /home/ericparsley/#
