
#Get disk names
sudo blkid | grep UUID=

#Make dirs to mount drives to
mkdir Data

#Open fstab file
sudo nano /etc/fstab

#Data SSD
UUID=aaa7bffc-6d7b-4ea6-bf68-045c11de9c3d /home/ericparsley/Data btrfs defaults 0 0

#Reload to use next command
sudo systemctl daemon-reload

#Refresh stuff so I don't have to restart anymore :D
sudo mount -a

#Gain ownership of drives
sudo chown -R -v ericparsley:ericparsley /home/ericparsley/Data

