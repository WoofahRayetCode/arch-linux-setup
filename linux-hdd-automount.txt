#FSTAB code for adding extra harddrives for automount
#I'm sure I'll be updating this a lot when I have to do reformats or get new hard drives

#Commands to run first
sudo blkid #Copy UUIDs
mkdir ~/Storage1TB
mkdir ~/Storage2TB
mkdir ~/Storage4TB
sudo mkdir /etc/backup
sudo cp /etc/fstab /etc/backup/fstab
sudo nano /etc/fstab

#Restore FSTAB
cd /etc/
sudo rm fstab
sudo cp /etc/backup/fstab /etc/

#Commands to allow full access to drives
sudo chown -v ericparsley:ericparsley /home/ericparsley/Storage1TB
sudo chown -v ericparsley:ericparsley /home/ericparsley/Storage2TB
sudo chown -v ericparsley:ericparsley /home/ericparsley/Storage4TB

#dev/sdb1
UUID=1150fd6c-7a29-4b8d-b4af-68365f16dbff       /home/ericparsley/Storage1TB       ext4       defaults,noatime       0       2

#dev/sdc1
UUID=26820c03-2ad9-4088-98c3-19b0f49e752d       /home/ericparsley/Storage2TB       ext4       defaults,noatime       0       2

#dev/sdd1
UUID=b1681a5f-72d1-46db-83c6-134355a39cd8       /home/ericparsley/Storage4TB       ext4       defaults,noatime       0       2
