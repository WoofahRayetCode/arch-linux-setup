#FSTAB code for adding extra harddrives for automount
#I'm sure I'll be updating this a lot when I have to do reformats or get new hard drives

#Commands to run first
sudo blkid #Copy UUIDs
mkdir ~/Games1TB
mkdir ~/Storage2TB
mkdir ~/Storage4TB
sudo mkdir /etc/backup
sudo cp /etc/fstab /etc/backup/fstab
sudo nano /etc/fstab

#Restore FSTAB
cd /etc/
sudo rm fstab
sudo cp /etc/backup/fstab /etc/

#Commands to allow full access to drives
sudo chown -v ericparsley:ericparsley /home/ericparsley/Games1TB
sudo chown -v ericparsley:ericparsley /home/ericparsley/Storage2TB
sudo chown -v ericparsley:ericparsley /home/ericparsley/Storage4TB

#dev/sdb1
UUID=9aa3a335-9d30-46b2-8996-6eccd35ca9a9       /home/ericparsley/Games1TB       ext4       defaults,noatime       0       2

#dev/sdc1
UUID=0827a303-b4da-48d2-af71-473e300002eb       /home/ericparsley/Storage2TB       ext4       defaults,noatime       0       2

#dev/sdd1
UUID=55c2db22-5fcf-4c71-bfaf-15ce0de38f67       /home/ericparsley/Storage4TB       ext4       defaults,noatime       0       2
