#Get disk names
sudo blkid | grep UUID=
#Make dirs to mount drives to
mkdir STUFF

#Open fstab file
sudo nano /etc/fstab
sudo gedit /etc/fstab

#Mount INTERNAL STORAGE HDD
UUID=3a2ac198-cfa4-4625-b4fc-fe2be0f9b717 /home/ericparsley/STUFF ext4 defaults 0 0

#Reload shit to use next command
sudo systemctl daemon-reload

#Refresh stuff so I don't have to restart anymore :D
sudo mount -a

#Gain ownership of HDD
sudo chown -R -v ericparsley:ericparsley /home/ericparsley/STUFF
