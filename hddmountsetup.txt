#Get disk names
sudo blkid
sudo blkid | grep UUID=
#Make dirs to mount drives to
mkdir GAMES
#SSD UUIDs
LABEL="Games" UUID="cdcc47eb-f751-419f-9750-cee8191d6b5f"

#Open fstab file
sudo nano /etc/fstab
sudo gedit /etc/fstab

#Mount SSD
UUID=cdcc47eb-f751-419f-9750-cee8191d6b5f /home/ericparsley/GAMES btrfs defaults 0 0
#Gain ownership of hdds and ssd
sudo chown -R -v ericparsley:ericparsley /home/ericparsley/GAMES

#Mount External HDD
UUID=D6A6B87DA6B86023 /home/ericparsley/EXTERNAL auto defaults
#Get ownership of External HDD
sudo chown -R -v ericparsley:ericparsley /home/ericparsley/EXTERNAL

#Refresh stuff so I don't have to restart anymore :D
sudo mount -a
